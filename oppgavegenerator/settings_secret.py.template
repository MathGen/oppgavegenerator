# Edit the settings and remove .template from filename.

# E-MAIL SERVER SETTINGS
EMAIL_USE_TLS = True
EMAIL_HOST = 'smtp.example.com'
EMAIL_HOST_USER = 'user@example.com'
EMAIL_HOST_PASSWORD = 'password'
EMAIL_PORT = 587

# The secret key is used for cryptography so it is important to keep it secret.
SECRET_KEY = 'insert secret key here' #django will not run without a secret key

# Edit this to your domain name or server IP-address
ALLOWED_HOSTS = ['localhost', 'yourserver']
